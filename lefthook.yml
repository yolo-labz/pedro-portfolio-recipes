# Git hook runner — faster than husky, single Go binary.
# Install:  brew install lefthook && lefthook install

commit-msg:
  commands:
    commitlint:
      run: npx --no -- commitlint --edit {1}
    no-ai-trailers:
      run: |
        if grep -qiE 'co-authored-by:[[:space:]]*(claude|opus|sonnet|gpt|codex)|generated with claude code|🤖' {1}; then
          echo "Remove the AI authorship trailer / robot emoji from the commit message" >&2
          exit 1
        fi

pre-commit:
  parallel: true
  commands:
    markdownlint:
      glob: "*.md"
      run: npx --no -- markdownlint-cli2 {staged_files}
    shellcheck:
      glob: "*.{sh,bash}"
      run: shellcheck --severity=warning {staged_files}
    shfmt:
      glob: "*.{sh,bash}"
      run: shfmt -i 2 -ci -bn -d {staged_files}
